export default function GraphTile({ children }) {
  return (
    <div
      style={{
        backgroundColor: "#202020",
        padding: "1.5rem",
        marginTop: "1rem",
      }}
    >
      {children}
    </div>
  );
}
